# ذاكرة مشروع منصة نور — Noor Platform

> **الغرض:** نقطة دخول مستدامة لأي نموذج يعمل على هذا المستودع. اقرأها كاملة قبل المراجعة أو التعديل، ثم افتح الملفات المشار إليها للتحقق من التفاصيل. هذه الذاكرة فهرس قرارات وحقائق، وليست بديلاً عن الكود أو الاختبارات.
>
> **آخر تحديث:** 2026-09-25 (Africa/Cairo).
>
> **تحديث 2026-09-25 (تنفيذ إصلاح فهارس المكتبة العامة):** فرع `fix/hf-index-strategy-and-tree-pagination` منشأ من `main@08fa47d`؛ commit الكود `d6105cb` (6 ملفات). أُضيف نمط فهرسة صريح `RepositorySource.indexMode` (`auto` | `static` | `tree`) مع `hf-telewat-dawah=static` و`hf-fatawa=tree` و`hf-islamic-books=tree`، فلم تعد تُطلب `fatawa/index.json` ولا `islamic_books/index.json`. صار `fetchMergedIndex` يتابع ترقيم Hugging Face Tree API عبر ترويسة `Link: rel="next"` بدل الاقتصار على أول 1000 مدخل، ويكتشف المستودعات الشجرية على مرحلتين: مسح متداخل محدود ثم مسح سطحي محدود عند عدم وجود JSON في المسح المتداخل. **المفحوص محليًا:** `npx vitest run src/__tests__` = 169/169 (منها 32 في `repository-routing.test.ts`، 11 جديدة)؛ `npx tsc --noEmit` على برنامج مقلّص يضم الملفات المتأثرة = 0 أخطاء؛ `npm run test:circular` = لا تبعيات دائرية؛ `scripts/test_huggingface_sync.mjs` = 18/18 عبر الشبكة الحقيقية (`hf-islamic-books` اكتشف **21** ملف JSON بعد أن كان **0**، و`hf-fatawa` 37، و`hf-telewat-dawah` 108). **لم يُتحقق من أي حالة PR أو نشر** في هذه الجلسة (لا يوجد `gh` في الطرفية، ولم يُفتح PR بعد). القياسات المذكورة تخص جهاز/شبكة هذه الجلسة.
>
> **لقطة سابقة (2026-09-23):** **أساس الكود المفحوص:** `main` و`origin/main` محليًا عند `08fa47dab3e3bf14544319144c72f378f64d8856` — commit دمج **PR #112 بأسلوب squash** من الرأس `fc98d7401b676b184167c08c93e6ba6fbb484490`. تحقق مستقل من GitHub: PR #112 `closed/merged` و`merge_commit_sha` مطابق. فرق `ac30b3e..main` محصور في `AGENTS.md` و`PROJECT_MEMORY.md`. فحوص `main` الأربعة ونجاح نشر Cloudflare **وردت في تقرير المنفذ ولم أتحقق منها مباشرة في هذه المراجعة**. لا تعتبر حالة PR أو النشر هنا آنية بعد هذا التاريخ.

## 1. بروتوكول استخدام الذاكرة وتحديثها

1. **قبل كل مراجعة:** اقرأ هذا الملف و`AGENTS.md`؛ تحقق من الفرع و`git status` والرأس الفعلي، ثم اقرأ diff والكود والاختبارات ذات الصلة. عند الحاجة إلى Next.js، اقرأ الدليل المناسب في `node_modules/next/dist/docs/` قبل كتابة الكود، وفق `AGENTS.md`.
2. **بعد كل مراجعة أو تغيير:** حدّث تاريخ/رأس اللقطة، حالة PR بعد التحقق منها، القرار وأدلته وقيوده في «سجل المراجعات»، وأي معلومة معمارية تغيرت. إذا لم يتغير شيء، أضف سطرًا مؤرخًا يوضح نطاق المراجعة ونتيجتها. لا تنسخ تقرير المنفذ بوصفه حقيقة مثبتة؛ ميز بين **مفحوص في الكود/اختبار/شبكة** و**مبلّغ عنه**.
3. **عند الاختلاف:** الكود الحالي والبيانات الفعلية ونتائج الاختبارات المباشرة أولى من هذا الملف، ثم حدّث الملف. لا تستنتج الدمج أو النشر من نجاح CI وحده. لا تغيّر `main` أو تنشر بسبب عبارة «جاهز»؛ اتبع تفويض المستخدم المحدد لكل مهمة.
4. **حجم الذاكرة:** حافظ على خريطة المشروع والقرارات المستمرة هنا؛ ضع تفاصيل التنفيذ الدقيقة في الملفات والاختبارات أو روابط PR. لا تخزّن أسرارًا أو رموز وصول أو مخرجات بيانات ضخمة.

## 2. هوية المشروع وطريقة تشغيله

| البند | الحالة المفحوصة |
|---|---|
| المستودع | `https://github.com/hozifa460/Noor-Platform`؛ الفرع الافتراضي `main` |
| المنتج | منصة إسلامية عربية RTL؛ مراكز القرآن والحديث والفتاوى والكتب والإذاعات والأذكار، مع صفحات للبحث والإعدادات وغيرها |
| الإطار | Next.js 16 App Router، React 19، TypeScript، Tailwind CSS 4، Zustand؛ راجع `package.json` للإصدارات الدقيقة |
| الصفحات | `src/app/`، بما فيها `/quran`، `/hadith`، `/fatwa`، `/books`، `/radio`، `/adhkar` |
| الشيفرة | ميزات مستقلة في `src/features/{quran,hadith,fatwa,books,radio,adhkar}`؛ الأدوات المشتركة في `src/lib/shared/` و`src/lib/arabic/` |
| الإنتاج المعروف | `https://noor-platform.pages.dev`؛ نُشرت إصدارات أيضًا على Vercel. رابط معاينة ذي معرف نشر ليس عنوان الإنتاج الدائم |
| النشر | `next.config.ts`: تصدير ثابت إلى `out/` عند `CF_PAGES`/`CLOUDFLARE_PAGES`؛ مسار Vercel/Node يحافظ على API route handlers؛ Cloudflare edge function في `functions/` |
| إعدادات الأمان | CSP ورؤوس أمنية في `next.config.ts`؛ على Cloudflare تُستخدم `public/_headers` |
| البناء | `npm run build` يمر عبر `scripts/build.mjs` إلى `tools/build/build.mjs` |

**أوامر الفحص الأساسية:** `npm run typecheck`، `npm run lint`، `npm run test:unit`، `npm run test:circular`، `npm run test:e2e`، `npm run build`. `npm test` يجمع فحص التبعيات الدائرية وVitest واختبارات `scripts/run-all-tests.mjs`. اختبر الملفات المتأثرة أولًا، ثم الفحوص المطلوبة؛ لا تعتبر اختبار jsdom إثباتًا لسلوك الرسم في متصفح حقيقي. `playwright.config.ts` يشغّل Chromium، ويبني CI التطبيق قبل اختبارات E2E. في `.github/workflows/ci.yml` تُشغّل اختبارات الوحدة **قبل** `npm run data:sync`؛ لذلك يجب أن تنجح اختبارات الوحدة على checkout نظيف دون الملفات الكبيرة المتجاهلة.

## 3. بيانات المشروع ومصادرها

| المجال | الملف/المصدر | قواعد مهمة |
|---|---|---|
| القرآن | `public/data/quran/surahs/{1..114}.json`، `quran_search_index.json`، `morphology/{1..114}.json` و`roots_index.json` | نص السور المعروض محفوظ برسمه؛ فهرس البحث والمعالجة الصرفية يُحمّلان عند الحاجة. مصادر الصرف: Quranic Arabic Corpus v0.4/جامعة ليدز وفق الواجهة والسكربت `scripts/generate-quran-word-morphology.py`. |
| القراء والروايات | `src/features/quran/domain/data.ts`، `public/data/quran/mp3quran_reciters.json`، `riwayaat_reciters_map.json` | 21 خيار رواية في `QIRAAT_LIST`؛ لا تخلط الرواية مع شخص القارئ. تسجيلات السور الكاملة من MP3Quran، وتسجيلات الآيات المنفردة من EveryAyah حيث تتوفر. |
| الحديث | `public/data/hadith/hadiths_core_index.json` | ملف Git متتبع بحجم **22,535,616 بايت**؛ يحتوي **41,676 سجلًا** بصيغة رباعية فعلية `[bookIndex,idInBook,chapterId,text]`، و`books` = 14 و`grades` = 4. الحقل الخامس للدرجة اختياري في المحلل. |
| كتب الحديث الكاملة | `noor-platform-hadith` على Hugging Face، مع fallback محلي/تراثي | كثير من ملفات الكتب الكبيرة مستثنى من Git في `.gitignore`؛ `src/features/hadith/infrastructure/loader.ts` يحمّل الأجزاء من المستودع المخصص، ثم بدائل عند الحاجة، مع تخزين في الذاكرة/IndexedDB. |
| المستودعات البعيدة | `src/lib/shared/data-base.ts` | قواعد مستقلة للفتاوى والحديث والكتب عبر `NEXT_PUBLIC_FATWA_BASE`، `NEXT_PUBLIC_HADITH_BASE`، `NEXT_PUBLIC_BOOKS_BASE`؛ الافتراضات تشير إلى مستودعات `hozifa1/noor-platform-*` على Hugging Face. `quran_and_sunnah/sunnahset` مصدر تراثي لبعض مسارات fallback. |
| الفتاوى | أجزاء/فهارس كبيرة على HF وبعض كتالوجات محلية | `src/features/fatwa/infrastructure/worker-client.ts` ومقابلها Web Worker. لا تفترض وجود كل ملفات البيانات في checkout نظيف. |

تُنشأ بعض الفهارس بسكربتات في `scripts/`؛ راجع مصدر كل ملف قبل إعادة توليده أو رفعه. لا تغيّر نصًا دينيًا أو حكمًا حديثيًا أو محاذاة مصدر بمطابقة تقريبية من غير دليل نصي ومصدري محدد. قيم الأداء القديمة مرتبطة بالجهاز والشبكة والضغط؛ لا تُعمّم.

## 4. قسم القرآن: السلوك الحالي والقيود العلمية

**الخريطة:** `src/features/quran/domain/` للأنواع والبيانات والتطبيع ومطابقة القراء؛ `infrastructure/` لتحميل النص والبحث والصرف والـ MP3Quran؛ `model/quran-store.ts` للحالة و`use-quran-audio.ts` و`use-riwayah-reciters.ts` للصوت والتسجيلات؛ `ui/QuranHubView.tsx` و`QuranSearchModal.tsx` و`WordExplorerDrawer.tsx` و`QiraahModal.tsx` و`ReciterModal.tsx` للواجهة.

- **النص الرقمي:** `hasDigitalText: true` لحفص في `QIRAAT_LIST`؛ الروايات الأخرى تُعرض عند توافرها بمصحف مصور/تسجيلات، ولا يجوز تقديم النص التفاعلي الحفصي باسم ورش أو غيره. مصحف حفص المصور `pdfUrl: null`، ولا يُستخدم ملف شعبة بديلًا. إضافة نص ورش الرقمي تحتاج توثيق الترخيص والمصدر، تدقيق فروق رسم وعدّ الآي، ومحاذاة الصوت، في عمل مستقل.
- **البحث:** يغطي 6,236 آية من **نص حفص**، ويدعم مرجعًا مثل `2:255` والأرقام العربية وأسماء السور والعبارات دون تشكيل؛ ترتيب المرجع قبل العبارة قبل اجتماع الكلمات. `quran_search_index.json` يحمّل كسولًا، ونتائج كثيرة تعرض بالتدرج. الاختيار ينتقل إلى `?surah=X&ayah=Y` ويبرز الآية، وBack/Forward يعيدان الوجهة. لا يبدأ الصوت تلقائيًا؛ الانتقال من رواية أخرى يوضح/يضبط نص حفص. انظر `quran-search-engine.ts` و`QuranSearchModal.tsx`.
- **تنقل السور والصوت:** `quran-store.ts` يستخدم `inFlightSurahFetches` لمنع تكرار الجلب، ومعرّف طلب مستقل لمنع اعتماد استجابة قديمة في A→B→A. `currentAudioSessionId` يبطل الانتقال الصوتي الآلي بعد الإيقاف/الإيقاف المؤقت/التنقل اليدوي. اختبر ترتيبَي وصول الطلبات عند تغيير هذا المنطق.
- **القراء:** قارئ الآيات وقارئ السورة الكاملة مساران مختلفان. `reciter-matching.ts` يستخدم جدول معرّفات موثقة، لا تطابق أسماء تخمينيًا. قد يملك القارئ أكثر من تسجيل (`moshafId`)، مثل محمد أيوب 109/320؛ عند المزامنة احتفظ بالتسجيل المختار 320. شرط التشغيل `riwayahId` مطابق للرواية والسورة ضمن `surahList`؛ إن غاب التسجيل يُعطل التشغيل ويُفرغ `audio.src`. تغيير القارئ أثناء التشغيل يخضع لحراسة طلبات `play()` القديمة و`NotAllowedError`. انظر اختبارات `reciter-matching.test.ts` و`tests/e2e/quran-reciter-switching.spec.ts`.
- **استكشف الكلمة:** كلمات الآية التفاعلية مرتبطة بتحليل صرفي لكل سورة وفهرس جذور؛ المطابقة النصية صارمة، وعند غياب المحاذاة يُعرض «غير متاح» بدل تخمين. البسملة المضمنة في 95:1 و97:1 والحالة المركبة 37:130 استثناءات موثقة. فصل `network_error` عن `not_found` مع إعادة المحاولة. لا تعِد بوحدة الجذر = وحدة المعنى. الدرج يعيد التركيز إلى الكلمة الأصلية ويحصر التركيز أثناء فتحه.
- **الجوال:** إصلاحات PR #103 عالجت تجاوز العرض/قص الآيات وأسماء الروايات المختصرة؛ اختبارات `tests/e2e/quran-mobile-layout.spec.ts` تغطي عروض 320–430px والخطوط 22/32/50px. زر المساعد العائم يختفي عند تقاطع عناصر التحكم أو تشغيل الصوت ويعود بزوال السبب. اختبارات CSS zoom ليست بديلًا مطابقًا لتكبير المتصفح الحقيقي أو أجهزة فعلية؛ لا تضمن سلامة «أي جهاز مهما كان حجمه» بلا فحصه.

## 5. قسم الحديث: سلامة الإسناد والبحث

**الخريطة:** `src/features/hadith/infrastructure/{loader,search,grade-engine,sharh,translations-engine,fake-engine}.ts`، المتجر `model/hadith-store.ts`، الواجهة `ui/HadithHubView.tsx` والتبويبات في `ui/detail-tabs/`.

- **الأحكام:** «مقطوع» وصف لطرف الإسناد، وليس ضعفًا بحد ذاته. «صحيح مقطوع» يبقى صحيحًا و«حسن مقطوع» يبقى حسنًا، و«مقطوع» وحده غير محدد. لا تستنتج الصحة من وجود الحديث في ديوان أو من اسم المؤلف؛ حالة الفاحص `found_in_corpus` تعني وجود موضع في الفهرس فقط. لا تُنشئ «متفق عليه» تلقائيًا.
- **الشروح:** `DOCUMENTED_SHARH_LINKS` في `sharh.ts` يحصر الربط المحلي الموثق بستة مفاتيح: `bukhari:1`, `bukhari:8`, `muslim:1`, `nawawi40:1`, `nawawi40:2`, `nawawi40:3`. معرّف `seed` يختلف عن معرّف HadeethEnc. أُزيلت روابط خاطئة (`muslim:8/16/1907`, `tirmidhi:2609`) بسبب اختلاف ترقيم النسخ. طلب كتاب بلا ربط موثق يعيد `null`. النص HTML الخارجي المعروض في `HadithSharhTab.tsx` يمر عبر `sanitizeTafsirHtml` قبل `dangerouslySetInnerHTML`.
- **الترجمات:** `CONCORDANCE_EVIDENCE_CATALOG` في `translations-engine.ts` يسمح بالإنجليزية فقط للأربعين النووية والقدسية والدهلوية كما هو موثق في الكود؛ بقية الكتب، ومنها البخاري ومسلم، `concordance_required` حتى بناء ربط نصي موثق. لا تصل ترجمة إلى حديث اعتمادًا على رقم طبعة أخرى وحده.
- **البحث الشامل:** `search.ts` يحمّل الفهرس المحلي ثم مرآة HF عند الفشل، يجمع الطلبات المتزامنة بـ `microIndexInFlight`، ويتحقق من شكل الحمولة **قبل** تخزينها. مهلة كل مصدر **90 ثانية** تشمل الترويسات والجسم؛ مصدران تسلسليان قد يستغرقان نحو 180 ثانية عند تعطلهما. الفشل يظهر خطأ مستقلًا وزر إعادة محاولة؛ الفهرس الفارغ السليم فقط يعطي نتائج فارغة. `hadith-store.ts` يحمي تحميل الكتاب بمعرّف جيل من وصول A→B→A المتأخر. البحث الشامل ما زال يحمّل الملف الكامل؛ PR #108 لم يسرّع النقل.
- **PR #111 مدمج في `main`:** أضيفت مراحل «تحميل الفهرس» و«تجهيز النتائج» وقراءة `ReadableStream` للتقدم. مع `br`/`gzip` أو غياب `Content-Length` المؤشر غير محدد النسبة؛ مع استجابة identity وطول صالح تعرض النسبة. القراءة تستمر إلى `done` حتى لو أعلن الخادم طولًا أصغر. حدث `preparing` يسبق `JSON.parse`، و`MessageChannel` يتيح فرصة للرسم لكنه لا يضمن paint فعليًا. يحصر `HadithHubView.tsx` النسبة عند 99% أثناء `download`، ويحوّل الشريط إلى مؤشر غير محدد إذا تجاوزت البايتات المقروءة الطول المعلن؛ أثبت اختبار الواجهة الموجّه هذا السلوك. لا تصف اختبار DOM بأنه إثبات رسم إطار فعلي.
- **الأداء المقاس سابقًا:** ملف الفهرس الخام 22.5MB، ونقل Brotli رُصد حول 4.72MB؛ زمن تنزيل واحد تراوح كثيرًا بين جلسات/أدوات مختلفة (ثوانٍ إلى أكثر من دقيقة). لا تعمم 70 ثانية على المستخدمين. مجرد إضافة ملف `.br` جانبي لا يثبت أن Cloudflare Pages سيقدمه للـ URL الأصلي؛ يتطلب آلية تقديم مثبتة. نسبة تقدم `fetch` لا تُحسب من طول جسم مضغوط لأن stream يُقرأ بعد فك الضغط.

## 6. الفتاوى والكتب وباقي الميزات

- **مزامنة المكتبة العامة (تنفيذ 2026-09-25):** `AppShell` يستدعي `useLibrarySync` على جميع الصفحات. صار لكل مستودع نمط فهرسة صريح `RepositorySource.indexMode`: `static` (ملفات فهرس ثابتة فقط) و`tree` (Hugging Face Tree API فقط) و`auto` (السلوك القديم: فهارس ثابتة ثم الشجرة). الإعدادات المضمّنة: `hf-telewat-dawah=static` لأنه يملك `index.json` حقيقيًا (108 ملفات)، و`hf-fatawa=tree` و`hf-islamic-books=tree` لأنهما بلا `index.json` — ومن هنا جاءت طلبات 404 التي كانت تُطلق في كل صفحة. `candidateIndexUrls` لا يولّد أي مرشّح `index.json` في نمط `tree`، ولا يستدعي الشجرة في نمط `static`. `fetchTreeListing` في `fetcher.ts` يتابع صفحات الشجرة عبر ترويسة `Link: rel="next"` (حد 5 صفحات، ويُقبل نطاق `huggingface.co` فقط)، و`discoverTreeFiles` يكتشف الشجرة على مرحلتين: مسح متداخل بصفحتين كحد أقصى، ثم — إذا لم يجد JSON — مسح سطحي BFS بحد 8 قوائم دلائل. `isValidRepository` يرفض `indexMode` غير معروف، و`migrateSavedRepositories` يعبّئ النمط للمستودعات الافتراضية المحفوظة مع الحفاظ على الفرع والمسار والمفاتيح وأي نمط اختاره المستخدم. المزامنة ما زالت تحمّل `primaryFiles.slice(0, 12)` فقط، بينما البحث العام والعدادات يقرآن `useLibraryStore`؛ اكتشاف ملف لا يثبت تحميل عناصره.
- **الفتاوى:** `worker-client.ts` يربط كل طلب بحث بمعرّف `requestId` فريد بدل نص الاستعلام؛ الـ Worker يعيده في النتيجة، ولذلك لا يعلق أحد طلبين متطابقين. اختبارات السباق في `src/features/fatwa/__tests__/worker-client-races.test.ts`.
- **الكتب:** `src/features/books/` يحوي بحث الكتالوج وقارئ النص/PDF وطبقة تحميل وتخزين. مسار `src/app/api/shamela-text/route.ts` موجود لـ Vercel؛ أي تغيير شبكة/إعادة توجيه يحتاج مراجعة الكود الفعلي وقياسًا، لا افتراض استغلال. البيانات الكبيرة على HF بحسب `data-base.ts`.
- **الإذاعات والأذكار:** `src/features/radio/` و`src/features/adhkar/`. لا تنقل تفاصيل أعداد أو توفر ملفات من README إلى حقيقة تشغيلية قبل التحقق؛ النص التسويقي قد لا يطابق حالة البيانات الحالية.
- **ملفات التوليد/الاختبار:** `scripts/` و`tools/` تشمل بناء الفهارس ومزامنة البيانات واختبارات التكامل. مجلدات `scratch/` والبيانات الكبيرة المستثناة في `.gitignore` ليست مصدرًا عامًا معتمدًا أو جزءًا من checkout CI.

## 7. تسلسل القرارات وطلبات السحب

هذه **سجلات تاريخية**؛ تحقق من GitHub قبل ذكر أي حالة بصيغة الحاضر.

| PR | النتيجة المعروفة | القرار/الأثر الدائم |
|---|---|---|
| #99 | دُمج تاريخيًا على `802c4fa` | سباق تحميل السور A→B→A وإبطال انتقال الصوت التلقائي عند الإيقاف/التنقل. |
| #100 | دُمج على `b324435` | بحث آيات حفص، المراجع، الروابط العميقة، الترتيب والتطبيع. |
| #101 | دُمج على `b176768` | مستكشف الكلمة والصرف والجذور ومحاذاة الكوربس. |
| #102 | دُمج على `f8412f0` | فصل الروايات والقراء والتسجيلات الجزئية وحراسة التلاوة. |
| #103 | دُمج تاريخيًا؛ ورد في سجل المراجعات السابقة أن أثره وصل إلى `6f84843` | إصلاح تخطيط القرآن على الجوال وزر المساعد وتكبير الخط؛ راجع GitHub لتأكيد SHA الدمج قبل الاقتباس. |
| #104 | دُمج تاريخيًا؛ ورد `e1ef5c0` | مزامنة قارئ الآيات والسور بمعرّفات موثقة، إبطال الصوت البائد، بقاء تسجيل أيوب 320. |
| #105 | دُمج تاريخيًا؛ ورد `b856ae7` | النزاهة العلمية في الأحكام والشروح والترجمات والفاحص. |
| #106 | دُمج على `ea6b04d` | تعقيم HTML الشرح قبل العرض. |
| #107 | دُمج على `bdb4055` | سباقات طلبات الفتاوى المتطابقة وتحميل كتب الحديث A→B→A. |
| #108 | دُمج على `e51106abaee058e6a1f7375c598754a72d002505` | تجميع جلب الفهرس، مهلة محدودة، تحقق الحمولة، فشل/إعادة محاولة منفصلان. |
| #111 | **مُدمج (squash) 2026-09-23** — commit الدمج `ac30b3e3e783a7fac68ea351db6375b02ae2e67e` من الرأس `52c3c1ec6923d0445ec2899daded647903403353` | مراحل انتظار البحث؛ أُغلقت ملاحظتا اكتمال القراءة وشريط التقدم، ودُمج بأمر المستخدم بعد إعادة تحقق كاملة (الرأس + الست فحوص). نجاح فحص Cloudflare تحققته مستقلاً؛ نجاح الفحص الوظيفي الإنتاجي ورد في تقرير المنفذ. |
| #112 | **مُدمج (squash) 2026-09-23** — commit الدمج `08fa47dab3e3bf14544319144c72f378f64d8856` من الرأس `fc98d7401b676b184167c08c93e6ba6fbb484490` | أُدخل `PROJECT_MEMORY.md` وبروتوكول قراءته وتحديثه في `AGENTS.md` إلى `main`. GitHub يؤكد الدمج؛ نجاح فحوص `main` الأربعة ونشر Cloudflare وردا في تقرير المنفذ. سجل المراجعة والدمج الحاليان إضافتان محليتان لم تُنشرَا بعد. |

لا تسجل #109/#110 أو أي PR مفقود بالاستنتاج؛ افحصه إن أصبح ذا صلة. نجاح معاينة Cloudflare على فرع PR ليس نشرًا إلى الإنتاج.

## 8. سجل المراجعات الجاري

| التاريخ | النطاق والرأس | ما فُحص | القرار/ما يتبقى |
|---|---|---|---|
| 2026-09-24 | مراجعة تقرير تشخيص فهارس HF؛ HEAD المحلي `fc98d74` ومرجعا main عند `08fa47d` | قرأت التقرير والذاكرة وAGENTS، وراجعت `repositories.ts`, `fetcher.ts`, `use-library.ts`, `SearchView.tsx` واستدعاء AppShell ونوع RepositorySource. تأكد تخمين index.json وrecursive=true وعدم متابعة pagination في fetchMergedIndex، وترحيل إعدادات localStorage الحالي لا يتضمن نمط الفهرسة الجديد. تقرير الشبكة والأعداد من المنفذ فقط. | الإصلاح المقترح مشروط بنمط فهرسة صريح وترحيل الإعدادات القديمة وحفظ الملفات المتداخلة المطلوبة أو توثيق حصر النطاق قبل إزالة recursive. لا تكفي إزالة console errors؛ يلزم اختبار الفهرسة والمسارات وفشل الشبكة. لا يُعمم عدم التأثر على البحث العام؛ التحميل الأولي محدود بـ12 ملفًا. لا كود تنفيذي عُدّل في هذه المراجعة. |
| 2026-09-24 | تحديد الخطوة التالية بعد #112 | قُرئت الذاكرة و`AGENTS.md` وحالة الشجرة؛ الفرع المحلي ما زال `docs/project-memory-agents` مع تعديل الذاكرة محفوظًا. لم تُعَد مراجعة GitHub أو قياس الإنتاج اليوم؛ لقطة الحالة أعلاه مؤرخة 2026-09-23. | الأولوية المقترحة: تشخيص طلبي 404 المبلغ عنهما لفهارس الفتاوى والكتب، وتتبع المستدعي والمصدر الصحيح وأثرهما على المستخدم قبل تحديد الإصلاح. لا تُعد المشكلة مثبتة حاليًا دون إعادة إنتاجها. يُرسل للمنفذ نطاق تشخيص محدود أولًا، مع حفظ الذاكرة وعدم دمج أو نشر. |
| 2026-09-24 | تشخيص طلبي 404 لفهارس الفتاوى والكتب (`fatawa/index.json` و`islamic_books/index.json`) | أُعيد الاختبار عبر Playwright في صفحات `/hadith` و`/fatwa` و`/books` على الإنتاج، ووفحصت مسارات Hugging Face مباشرة وكود `candidateIndexUrls` و`useLibrarySync`. الحقيقة المثبتة: الطلبان يخرجان من `useLibrarySync` في `AppShell` (يُشغّل في كل صفحات الموقع عبر `layout.tsx`)؛ السبب هو تخمين `index.json` الثابت لمستودعي `fatawaset` و`islamic_books` اللذين لا يحتويان على `index.json` بل يُفهرسان ديناميكيًا عبر HF tree API. في `fatawaset` ينجح fallback الـ tree ويكتشف 37 ملفًا. أما في `islamic_books` فـ HF tree API الممرر له `recursive=true` يعيد فقط أول 1000 مدخل (كلها مجلدات تصنيفات وشاملة) فيخرج 0 ملفات JSON للكتب، بينما شجرة المجلد `books` المباشرة تحوي 21 ملف JSON لـ islamhouse. مراكز `/hadith` و`/fatwa` و`/books` تستخدم محركاتها المعزولة المستقلة (`FATWA_BASE` و`BOOKS_BASE`) وتعمل بكفاءة كاملة. | التشخيص مثبت بالدليل؛ وُثِّق أصغر إصلاح مقترح في التقرير مع الإبقاء على الشجرة نظيفة ودون دمج أو نشر. |
| 2026-09-25 | تنفيذ إصلاح طلبي 404 (`fatawa/index.json` و`islamic_books/index.json`) — فرع `fix/hf-index-strategy-and-tree-pagination`، commit `d6105cb` من `main@08fa47d` | عُدِّل `src/lib/types.ts` (إضافة `indexMode`)، `src/lib/shared/repositories.ts` (إعدادات افتراضية صريحة + `huggingfaceTreeUrl` + `isHuggingFaceTreeUrl` + `parseNextLink` + تحديث `candidateIndexUrls` والتحقق والترحيل)، `src/lib/shared/fetcher.ts` (متابعة ترقيم `Link` + `discoverTreeFiles` + `discoverRepoFiles`)، `src/components/library/SettingsView.tsx` (منتقي نمط الفهرسة)، `src/__tests__/repository-routing.test.ts` (11 اختبارًا جديدًا) و`scripts/test_huggingface_sync.mjs`. أُعيد إنتاج ترويسة HF الحقيقية: `Link: <...?recursive=true&limit=1000&cursor=...>; rel="next"` عند تجاوز 1000 مدخل. الاختبارات التي شُغلت فعلًا: `npx vitest run src/__tests__` = **169/169** (منها 32 في ملف التوجيه)، `npx tsc --noEmit` على `tsconfig` مقلّص للملفات المتأثرة = **0 أخطاء** (لم يكتمل فحص المشروع الكامل داخل مهلة الطرفية الحالية)، `npm run test:circular` = نظيف، `npx tsx scripts/test_huggingface_sync.mjs` = **18/18** عبر الشبكة الحقيقية. | الإصلاح منفَّذ ومقيس: `hf-islamic-books` اكتشف 21 ملف JSON بعد أن كان 0، و`hf-fatawa` بقي 37، و`hf-telewat-dawah` بقي 108، ولا يولّد أي مستودع شجري مرشّح `index.json` (لا 404). مخاطر متبقية معلنة: (1) بعض حمولات الفتاوى ملفات Git LFS، ومسار `/raw/` يعيد مؤشر LFS لا المحتوى — لم يُغيَّر داخل هذا النطاق؛ (2) الفحص البصري في متصفح حقيقي على فرع PR لم يُجرَ؛ (3) لم يُفتح PR ولم يُدمج ولم يُنشر (لا يوجد `gh` في الطرفية). |


| 2026-09-23 بعد تقرير دمج #112 | متابعة PR #112 و`main@08fa47dab3e3bf14544319144c72f378f64d8856` | قُرئت الذاكرة و`AGENTS.md` قبل التحديث. GitHub connector أكد `closed/merged`، رأس PR `fc98d74` و`merge_commit_sha=08fa47d`. محليًا `main=origin/main=08fa47d` و`git show`/`git diff ac30b3e..main` يؤكدان ملفين توثيقيين فقط ووجود كتلة Next.js الأصلية. تقرير المنفذ يذكر 4/4 فحوص `main` ناجحة ونشر Cloudflare ناجحًا؛ لم أتحقق من هذين الادعاءين مباشرة في هذه المتابعة. | الدمج مثبت. بقي تعديل `PROJECT_MEMORY.md` المحلي غير ملتزم ويتضمن نتيجة المراجعة السابقة وهذا السطر؛ احفظه في تحديث توثيقي لاحق ولا تقدمه بوصفه جزءًا من commit الدمج. |
| 2026-09-23 | مراجعة مستقلة لـPR #112 عند `fc98d7401b676b184167c08c93e6ba6fbb484490` | قُرئت الذاكرة و`AGENTS.md` كاملين قبل المراجعة. Git diff من `main@ac30b3e`: ملفان توثيقيان فقط (`AGENTS.md` و`PROJECT_MEMORY.md`)، وكتلة Next.js لم تتغير، و`git diff --check` نظيف. GitHub API: الرأس/الأساس مطابقان، PR `open`, `merged=false`, `mergeable=true`, وست فحوص completed/success. لم أكرر اختبارات التطبيق محليًا لعدم تغير كوده. | لا يظهر مانع لدمج PR التوثيقي. هذا الصف تحديث محلي بعد رأس `fc98d74`، وليس جزءًا من CI المفحوص؛ يجب عدم تقديمه كجزء من PR الحالي حتى يُلتزم به في تحديث لاحق. |
| 2026-09-23 بعد تقرير دمج #111 | مراجعة حالة الدمج والذاكرة | قُرئت الذاكرة و`AGENTS.md` قبل المراجعة. GitHub API: PR #111 `closed/merged`، merge commit و`main` كلاهما `ac30b3e3e783a7fac68ea351db6375b02ae2e67e`، وفحوص main الأربعة completed/success. محليًا `main=origin/main` على الرأس نفسه، و`git show` يؤكد squash للتغييرات. لم أُعِد تشغيل فحص المتصفح الإنتاجي؛ نتائجه في الصف السابق من تقرير المنفذ. | الدمج مؤكد. `PROJECT_MEMORY.md` ما زال **غير متتبع في Git** و`AGENTS.md` تعديلًا محليًا؛ لذلك لا تصل تعليمات الذاكرة إلى clone آخر حتى يُضمّ الملفان في commit توثيقي مستقل. |
| 2026-09-23 03:35 +03:00 | دمج PR #111 والتحقق من الإنتاج — squash commit `ac30b3e3e783a7fac68ea351db6375b02ae2e67e` | أُعيد التحقق قبل الدمج: الرأس `52c3c1ec6923d0445ec2899daded647903403353` مطابق والست فحوص على PR success. مُرِّ `POST /pulls/111/merge` بـ404 متكرر رغم صلاحية admin ونطاق repo (سبب مجهول — موثّق هنا) فدُمج عبر GraphQL `mergePullRequest` بأسلوب SQUASH. فحوص main بعد الدمج 4/4 success: Lint/Test/Audit & Build، Playwright E2E، CodeQL Analysis، Cloudflare Pages (نشر الإنتاج). `main` المحلي حُدِّث بـ`git fetch origin main:main` وأصبح `ac30b3e`. فحص وظيفي `scratch/prod-smoke.mjs` على `https://noor-platform.pages.dev/hadith` بسياق متصفح نظيف: تسلسل مثبت عبر MutationObserver download «جارٍ تحميل فهرس البحث» ← preparing «جارٍ تجهيز النتائج» ← results «نتائج البحث الشامل: ٣٥ حديث»، بلا تعليق ولا pageerror ولا صندوق خطأ. شُهِد console-error×2 فقط: 404 مسبق خارج النطاق لـ`fatawa/index.json` و`islamic_books/index.json` على huggingface (مصادر الكتب/الفتاوى في `data-base.ts`/`use-library.ts` — لم يمسّها هذا الـPR). | الدمج مكتمل وPR #111 `closed/merged`؛ مخاطر متبقية معلنة: غموض 404 في REST merge (وثّق fallback GraphQL)، و404 HF المسبق غير المرتبط. |
| 2026-09-23 03:23 +03:00 | مراجعة مستقلة للرأس `52c3c1e` في PR #111 | قُرئت الذاكرة قبل المراجعة؛ فُحص diff الملفات الأربعة، وحالة GitHub (`open`, `merged=false`, `mergeable=true`) وست فحوص مكتملة ناجحة؛ أُعيد تشغيل اختبارات المحرك والمراحل والواجهة والتجاوز محليًا: **26/26**؛ `git diff --check` نظيف. | لا يظهر مانع وظيفي جديد في النطاق المراجع. سقف 99% والتحول إلى مؤشر غير محدد مثبتان؛ ترتيب DOM مثبت، والرسم الفعلي في المتصفح غير مثبت ولا يدّعيه الكود الآن. لم أكرر Vitest الكامل أو الفحص البصري للإنتاج. تُرك PR مفتوحًا بلا دمج أو نشر. |
| 2026-09-23 03:20 +03:00 | تنفيذ ملاحظة PR #111 — رأس جديد `52c3c1ec6923d0445ec2899daded647903403353` (من `9f91b1b`) | عُدِّل `src/features/hadith/ui/HadithHubView.tsx`: سقف 99% أثناء مرحلة `download` (لأن `done === true` لم يتحقق بعد — `preparing` تُعلن بعد اكتمال القراءة فقط)، وفرع غير محدد عند `loadedBytes > totalBytes`. عُدّلت تعليقات `search.ts` و`hadith-store.ts`: `Content-Length` لعرض فقط، وMessageChannel يمنح المتصفح فرصة للرسم ولا يضمنه. أُنشئ `hadith-progress-overrun-ui.test.tsx` (محرك/مخزن/مكوّن حقيقيون + stream مُدار يدويًا وإعلان طول = الجزأين 1+2): يثبت ظهور 99% عند تساوي المقروء، غياب 100% قبل `done`، التحول لمؤشر موجَّه بعد التجاوز، وظهور «جارٍ تجهيز النتائج» في DOM قبل النتائج (ترتيب DOM فقط، لا paint مؤكد). اختبارات شُغّلت فعلًا: مستهدفة 26/26، vitest كامل 531/531، `tsc --noEmit` نظيف، ESLint 0 أخطاء (4 تحذيرات `scratch/*.mjs` قائمة سلفًا)، madge لا تبعيات دائرية. CI المتحقق منه على الرأس: Lint/Test/Audit & Build + Playwright E2E + CodeQL + CodeQL Analysis (js/ts) + Cloudflare Pages + Vercel — كلها success. PR بعد التحقق: `open`, `merged=false`, `mergeable_state=clean`. | أُغلقت الملاحظة وأُبقي PR #111 مفتوحًا دون دمج أو نشر بانتظار إعادة المراجعة. خطر متبقٍّ معلن: اختبار jsdom يثبت ترتيب DOM لا أن المتصفح رسم إطارًا فعلًا. |
| 2026-09-23 | PR #111، `9f91b1b` | GitHub: `open`, `merged=false`, `mergeable=true`، ست فحوص ناجحة. Diff الكود والاختبارات؛ محليًا 25/25 لاختبارات `hadith-micro-index-races`, `hadith-global-search-stages-ui`, `hadith-global-search-ui`، والشجرة كانت نظيفة قبل إنشاء ملف الذاكرة. | الإصلاحان الأساسيان صحيحان. الشريط قد يصل 100% قبل اكتمال القراءة عند طول ناقص؛ اختبار DOM لا يثبت paint حقيقي. أُرسل للمنفذ طلب تعديل محدود مع إبقاء PR مفتوحًا. |
| 2026-09-23 | إنشاء هذه الذاكرة | فُحصت `AGENTS.md`, `package.json`, `next.config.ts`, `.github/workflows/ci.yml`, بنية `src/features`, ملفات القرآن والحديث والفهرس الفعلي، ورأسا GitHub `main` وPR #111. | أُضيف بروتوكول القراءة قبل كل مراجعة والتحديث بعدها في `AGENTS.md`. أي نموذج لاحق يجب تحديث هذا السجل بعد عمله. |

## 9. قالب تحديث سريع بعد مراجعة لاحقة

أضف صفًا إلى السجل يتضمن: **التاريخ والتوقيت/المنطقة، PR والـ SHA الدقيق، الملفات أو diff التي قرئت، الاختبارات التي شُغّلت فعلًا، حالة CI/النشر التي تحققت منها فعلاً، القرار والمخاطر المتبقية**. حدّث رأس اللقطة وأقسام الميزات المتأثرة. لا تُبدّل قياسًا مبلّغًا عنه إلى قياس أجريته بنفسك، ولا تخلط المعاينة مع الإنتاج.
