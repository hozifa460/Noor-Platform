export interface BookCategory {
  id: string;
  name: string;
  emoji: string;
  description: string;
}

export interface BookLanguage {
  code: string;
  name: string;
  nativeName: string;
  flag: string;
}
